# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/zen-progress-tracker

# JWT Configuration
JWT_SECRET=your-secure-jwt-secret-key-here

# Server Configuration
PORT=5000
NODE_ENV=development

# Client URL for CORS
CLIENT_URL=http://localhost:5173

# Production Client URL (for Render + Netlify)
# CLIENT_URL=https://zen-progress-tracker.netlify.app
